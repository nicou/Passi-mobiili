<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fi.softala.passi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="Työkykypassi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.KirjautumisActivity"
            android:label="Kirjautuminen" />
        <activity android:name=".activities.ValikkoActivity" />
        <activity android:name=".activities.TehtavakortinValintaActivity" />
        <activity android:name=".activities.Tehtavakortti" />
        <activity android:name=".activities.ryhmatActivity"></activity>
    </application>

</manifest>